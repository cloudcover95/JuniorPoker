# JuniorPoker

MIT. Local table: multi-deck shuffle (C), deal, RFID *interface*, cloud-bubble peek.
Rust is a later kernel rail. No USB driver ships. Loopback Home pointer only.

```bash
make -C c
PYTHONPATH=. python3 scripts/table_prod.py --seats 6 --decks 2
PYTHONPATH=. python3 tests/test_table.py
```
