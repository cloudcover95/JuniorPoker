# Under the table

Raspberry Pi or any SFF PC. Run `scripts/table_prod.py` on 127.0.0.1.
RFID reader: later `/dev/ttyAMA0` or USB HID. Do not open the port from Home automation.
Shuffle .so builds on aarch64 the same as x86.
